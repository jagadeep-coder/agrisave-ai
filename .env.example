# AgriSave AI – Frontend Environment Variables
# Copy this file to .env and set the backend URL.
# The Gemini API key lives ONLY in server/.env — never here.

# URL of the AgriSave AI Express backend (no trailing slash)
VITE_API_URL=http://localhost:5000
